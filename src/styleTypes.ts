export enum PaperElevation {
  BOTTOM = 0,
  LOW = 3,
  MIDDLE = 5,
  HIGH = 10,
}

export enum FontSize {
  LOGIN_HEADER = "2rem",
  STONE_NAME = "3rem",
}

export enum BackgroundColor {
  CONTENTS_BG = "#C0C0C0", //구역식별용 임시
}

export enum Border {
  DEFAULT_BORDER = "1px solid #bbbbbb",
}

export enum Padding {
  LOGIN_CONTAINER = "30px 20px 30px 20px",
  CONTENTS_CONTAINER = "50px",
}
